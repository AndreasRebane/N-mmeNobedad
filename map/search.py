from coordinates import *


def findClosestMatch(searchString: str):
    closestMachIDList 
    
    
    for x in LISTNAMEHERE:
        if (searchString.lower().find(x.officialName.lower())):
            closestMachIDList.append(x.id)
            


        for subNames in x.
        if (searchString.lower().find())
    
